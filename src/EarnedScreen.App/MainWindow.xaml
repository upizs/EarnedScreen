<Window x:Class="EarnedScreen.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EarnedScreen.App"
        mc:Ignorable="d"
        Title="EarnedScreen" Height="560" Width="440"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="EarnedScreen" FontSize="28" FontWeight="Bold"/>
        <StackPanel Grid.Row="1" Margin="0,8,0,16">
            <TextBlock x:Name="StatusText" FontSize="14" Foreground="#555"/>
            <TextBlock x:Name="DnsStatusText" FontSize="12" Foreground="#2E7D32" Margin="0,4,0,0"/>
        </StackPanel>

        <Border Grid.Row="2" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Pay the toll to earn your session:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <StackPanel x:Name="ChecklistPanel"/>

                    <TextBlock x:Name="NotionHeader" Text="Today's tasks (Notion):" FontWeight="SemiBold"
                               Margin="0,16,0,8" Visibility="Collapsed"/>
                    <StackPanel x:Name="NotionPanel"/>
                    <TextBlock x:Name="NotionNote" Margin="0,10,0,0" FontSize="12" Foreground="#999"
                               TextWrapping="Wrap" Visibility="Collapsed"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" x:Name="MessageText" Margin="0,14,0,10" TextWrapping="Wrap" Foreground="#333"/>

        <Button Grid.Row="4" x:Name="EarnButton" Content="Earn Screen Time"
                Height="48" FontSize="16" FontWeight="SemiBold" Click="EarnButton_Click"/>
    </Grid>
</Window>
